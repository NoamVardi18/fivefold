# fivefold — CLAUDE.md

**Identity:** read `~/.claude/NOAM-PROFILE.md` first — who Noam is, how to work with him, and how this space connects to the others.

## What this is
A done/live capability-showcase site — five self-contained rooms, each arguing a different design philosophy (3 EN, 2 HE/RTL). No frameworks, no build step: every room is a single hand-written HTML file.

## Hard rules
1. **Cite only the real canonical Vercel URL** — the bare `fivefold.vercel.app` is a squatter site (Memory.md 2026-07-17), never cite or link it; verify the live URL before sharing one.
2. **Deploy from disk via the authed Vercel CLI** (proven path for this project, `projectName: fivefold`).
3. This is a done/live project — treat changes as polish, not a rebuild; see `KNOWLEDGE.md`.
